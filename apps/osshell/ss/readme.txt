
#important:
There is TWO kinds of window: 
One created by the display server for the client-side GUI application 
and another one created by the application inside a private canva.
So, there is TWO function to create a window.
>> This way we will draw windows inside the 3D scene,
   using the 2D routines.

--------------------
Now in the browser application 
i can call all the functionalities that belongs to the engine 
that is embedded into the browser.

--------------------
2D graphics/ 3D graphics using integers/ 3D graphics using floating point and Windows.
All this features are inside the engine. and ... 
we also can call the functionalities offered by the window server via requests.

